<Application x:Class="CodexQuotaWidget.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- ThemeService replaces these resources when Windows changes theme. -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#66F4F6F7" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#B3FAFBFB" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#171A1F" />
        <SolidColorBrush x:Key="ReadoutTextBrush" Color="#1F1F1F" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#66717A" />
        <SolidColorBrush x:Key="DividerBrush" Color="#80DCE2E6" />
        <SolidColorBrush x:Key="ProgressTrackBrush" Color="#99DCE2E6" />
        <SolidColorBrush x:Key="DataAccentBrush" Color="#4F6EF7" />
        <SolidColorBrush x:Key="StatusLiveBrush" Color="#0B7A49" />
        <SolidColorBrush x:Key="StatusWarningBrush" Color="#C4841D" />
        <SolidColorBrush x:Key="StatusOfflineBrush" Color="#77828A" />
        <SolidColorBrush x:Key="DangerBrush" Color="#B5463A" />
        <SolidColorBrush x:Key="PillBackgroundBrush" Color="#B8DDF2E7" />
        <SolidColorBrush x:Key="PillForegroundBrush" Color="#0B7A49" />
        <SolidColorBrush x:Key="HoverBrush" Color="#99E7EBEE" />
        <SolidColorBrush x:Key="WindowBorderBrush" Color="#66DCE2E6" />

        <FontFamily x:Key="DisplayFont">Bahnschrift, Segoe UI Variable Display, Segoe UI</FontFamily>
        <FontFamily x:Key="BodyFont">Segoe UI Variable Text, Segoe UI</FontFamily>
        <FontFamily x:Key="DataFont">Consolas</FontFamily>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFont}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <Style x:Key="QuotaProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="3" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="PART_Track" Height="3" SnapsToDevicePixels="True">
                            <Border Background="{DynamicResource ProgressTrackBrush}" CornerRadius="1.5" />
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{DynamicResource DataAccentBrush}"
                                    CornerRadius="1.5" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Padding" Value="0,-1,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="{DynamicResource BodyFont}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonSurface"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonSurface" Property="BorderBrush" Value="{DynamicResource DataAccentBrush}" />
                                <Setter TargetName="ButtonSurface" Property="BorderThickness" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
